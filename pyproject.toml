[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trace2train-core"
version = "0.1.0"
description = "Mini autograd + trace-to-train core"
requires-python = ">=3.10"

[project.scripts]
trace2train = "trace2train.cli:main"
